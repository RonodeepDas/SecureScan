# SecureScan task list

## Completed

- Backend skeleton created with FastAPI, encryption utilities, models, scanner, and service routes.
- Frontend React + Tailwind app created with password checker and port scanner tabs.
- HTTPS dev certificate support and self-signed cert generation added.
- SQLite-backed encrypted history storage implemented.
- Security validation for scan target, authorization gate, and port cap is in place.
- README and backend configuration files are populated.

## Remaining validation

- Install compatible backend packages for the current Python runtime.
- Run the backend test suite and resolve any failing cases.
- Run the frontend production build and fix any issues.
- Confirm the app works over HTTPS locally with self-signed certificate warnings expected.
